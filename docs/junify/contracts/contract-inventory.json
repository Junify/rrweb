{
  "schema_version": 1,
  "target": "Junify rrweb upstream 2.1.1 recorder/replayer compatibility",
  "baseline_commit": "3deb6e7da4528ddb33b5b7ff6a3e805d4ed14930",
  "generated_from": [
    "docs/superpowers/specs/2026-08-17-junify-upstream-2.1.1-design.md",
    "rrweb worktree census",
    "browser_extension worktree census at 1d8b3345ec9aee8d0c6c5d56d74c805a623b2a1a",
    "junify_rails worktree census at 2c1377da10f4db737e36be2cb59c663056132aee",
    "read-only session_recording_service census"
  ],
  "contracts": [
    {
      "id": "junify.canvas.mv3-processor",
      "title": "MV3-compatible injectable Canvas bitmap processor",
      "source": ["external-boundary", "observed-code", "explicit-doc"],
      "status": "red-known-risk",
      "recommended_layers": ["integration", "e2e"],
      "entrypoints": ["packages/rrweb/src/record/index.ts:record", "../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/imageBitmapProcessor.ts:createCanvasBitmapProcessor"],
      "observable_effects": ["packaged MV3 worker execution", "inline fallback output", "serialized Canvas mutations"],
      "expected_current_behavior": "The Junify package accepts an injected ImageBitmap processor, transfers bitmaps through a packaged worker under MV3 strict CSP, and falls back inline without changing the event schema.",
      "risk_if_broken": "The production extension loses Canvas recording, violates MV3 CSP, or emits incompatible events.",
      "gate_id": "G-CANVAS-PROCESSOR",
      "test_notes": "Unmodified 2.1.1 exposes only its inline-worker path; the Junify injectable API and focused tests do not exist yet."
    },
    {
      "id": "junify.canvas.non-destructive-capture",
      "title": "Canvas capture is non-destructive and closes bitmap resources",
      "source": ["explicit-doc", "external-boundary", "observed-code"],
      "status": "red-known-risk",
      "recommended_layers": ["integration", "e2e"],
      "entrypoints": ["packages/rrweb/src/record/observers/canvas/canvas-manager.ts", "../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/canvasBitmapWorker.ts"],
      "observable_effects": ["application Canvas pixels", "replayed Canvas pixels", "ImageBitmap lifetime", "worker lifetime"],
      "expected_current_behavior": "Recording leaves application Canvas 2D/WebGL pixels unchanged, reproduces pixels on replay, closes transferred bitmaps, and stops worker/RAF activity when recording stops.",
      "risk_if_broken": "Recording mutates customer pages or leaks GPU/bitmap resources during long sessions.",
      "gate_id": "G-CANVAS-PIXELS",
      "test_notes": "The Junify Canvas change contains the non-destructive concept, but the required pixel and cleanup assertions are missing."
    },
    {
      "id": "junify.compatibility.candidate-recorder",
      "title": "Candidate recorder replays in both candidate Junify surfaces",
      "source": ["explicit-doc", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["e2e", "golden/replay"],
      "entrypoints": ["packages/junify-compatibility/test/record-fixtures.test.ts", "../junify-rails-rrweb-2.1.1/frontend_v2/src/pages/monitors/sessionRecording/player.tsx", "../junify-rails-rrweb-2.1.1/frontend_rrweb_replayer/replay.js"],
      "observable_effects": ["main-player replayed DOM and Canvas", "legacy-static-player replayed DOM and Canvas"],
      "expected_current_behavior": "Artifacts from @junify-app/rrweb@2.1.1-junify.0 replay in the upgraded Rails main player and the explicitly retained static-player surface.",
      "risk_if_broken": "Deploying the recorder after the replayers creates recordings that Junify cannot inspect.",
      "gate_id": "G-COMPAT-CANDIDATE",
      "test_notes": "Task 2 adds an official rrweb@2.1.1 real-browser baseline, not the future @junify-app/rrweb@2.1.1-junify.0 candidate. The candidate producer and both cross-repository browser specs remain required."
    },
    {
      "id": "junify.compatibility.historical-replay",
      "title": "Historical alpha.4, alpha.19, and alpha.20 artifacts remain replayable",
      "source": ["explicit-doc", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["e2e", "golden/replay"],
      "entrypoints": ["packages/junify-compatibility/test/replay-matrix.test.ts", "../junify-rails-rrweb-2.1.1/frontend_v2/src/lib/react-rrweb-player/index.tsx", "../junify-rails-rrweb-2.1.1/frontend_v2/src/lib/react-rrweb-player/PlayerFrame.tsx", "../junify-rails-rrweb-2.1.1/frontend_rrweb_replayer/replay.js"],
      "observable_effects": ["visible replay DOM", "Canvas pixels", "seek result", "player completion"],
      "expected_current_behavior": "The candidate replayers consume provenance-locked fixtures produced by rrweb@2.0.0-alpha.4, @junify-app/rrweb@2.0.0-alpha.19, and @junify-app/rrweb@2.0.0-alpha.20.",
      "risk_if_broken": "Existing stored customer sessions become unreadable after the player upgrade.",
      "gate_id": "G-COMPAT-HISTORICAL",
      "test_notes": "Task 2 binds alpha.4, Junify alpha.19, and Junify alpha.20 loaded UMD bytes to registry-tarball integrity in yarn.lock, regenerates normalized real-browser artifacts into temporary storage, and replays marker-terminated slices plus full artifacts under official rrweb@2.1.1 in Chrome 151 with no skips. SPA/seek-ready evidence requires target-ID type-3 mutations; stylesheet stages assert computed styles. Task 4 seeks all four accepted artifacts forward and backward with the candidate bundle. Both candidate Rails player surfaces remain required before promotion."
    },
    {
      "id": "junify.compatibility.large-snapshot",
      "title": "Approximately 13.6 MB CSS FullSnapshot survives transport and replay",
      "source": ["explicit-doc", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["integration", "golden/replay"],
      "entrypoints": ["packages/junify-compatibility/test/large-snapshot.test.ts", "../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/v2SessionRecordingFlusher.ts"],
      "observable_effects": ["event count", "FullSnapshot indexes", "fragment reassembly", "payload digest", "gzip round trip"],
      "expected_current_behavior": "A deterministic roughly 13.6 MB CSS FullSnapshot preserves decoded event count, indexes, bytes, fragment ordering, and gzip digest.",
      "risk_if_broken": "Large real pages produce truncated, reordered, or unreplayable sessions.",
      "gate_id": "G-LARGE-SNAPSHOT",
      "test_notes": "Task 2 records a deterministic 13,600,000-byte CSS FullSnapshot with official rrweb@2.1.1 and locks event count, FullSnapshot index/payload digest, raw/gzip digests, and gzip round trip. Extension UTF-8 fragmentation/reassembly remains required before promotion."
    },
    {
      "id": "junify.compatibility.wire-format",
      "title": "Serialized rrweb event format remains unchanged",
      "source": ["explicit-doc", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["integration", "golden/replay"],
      "entrypoints": ["packages/rrweb/src/record/index.ts:record", "packages/junify-compatibility/test/record-fixtures.test.ts"],
      "observable_effects": ["decoded event sequence", "event count", "FullSnapshot payload", "event JSON schema"],
      "expected_current_behavior": "For fixed scenarios, decoded events remain wire-compatible; only documented upstream correctness changes may alter payload values.",
      "risk_if_broken": "The service and existing replayers reject or misinterpret newly recorded sessions.",
      "gate_id": "G-WIRE-FORMAT",
      "test_notes": "Task 2 authenticates each loaded bundle against its integrity-locked registry tarball, regenerates four comprehensive producers into temporary storage for timestamp-normalized event comparison, and locks browser provenance, event count, FullSnapshot indexes/payload digests, raw/gzip hashes, target-ID mutations, and replayed stage sinks. The future Junify candidate differential and both consumer surfaces remain required before promotion."
    },
    {
      "id": "junify.extension.persistence",
      "title": "Extension events reach real Chrome storage before ingest",
      "source": ["observed-code", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["integration", "e2e", "workflow-contract"],
      "entrypoints": ["../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/index.ts:post", "../browser-extension-rrweb-2.1.1/src/foreground/service/sso/sessionRecorder.ts:SessionRecorder", "../browser-extension-rrweb-2.1.1/src/background/repository/SessionRecordingEventRepository.ts:SessionRecordingEventRepositoryImpl"],
      "observable_effects": ["per-recorder CustomEvent", "runtime port payload", "Chrome storage keys and values", "delete/retry behavior"],
      "expected_current_behavior": "Monotonic indexed events cross the isolated-world bridge, runtime port, and real Chrome storage without loss, duplication, or reordering before V1/V2 flushing.",
      "risk_if_broken": "Unit-green recording silently loses or reorders persisted production events.",
      "gate_id": "G-EXT-PERSISTENCE",
      "test_notes": "Existing integration coverage reaches injection/overlay/mutations but does not observe worker execution, real Chrome storage, or ingest payload."
    },
    {
      "id": "junify.lifecycle.recorder-stop",
      "title": "Recorder stop releases all observers and asynchronous Canvas work",
      "source": ["observed-code", "explicit-doc"],
      "status": "red-known-risk",
      "recommended_layers": ["integration", "e2e"],
      "entrypoints": ["packages/rrweb/src/record/index.ts:record", "packages/rrweb/src/record/observer.ts:initObservers", "packages/rrweb/src/record/observers/canvas/canvas-manager.ts"],
      "observable_effects": ["no events after stop", "listener/observer counts", "Canvas RAF and worker shutdown", "released iframe/stylesheet/shadow-root references"],
      "expected_current_behavior": "Stop is idempotent, emits nothing afterward, and repeated start/stop churn does not retain observers, mirrors, RAF callbacks, workers, or detached documents.",
      "risk_if_broken": "Long sessions leak memory/CPU and stopped sessions continue collecting customer activity.",
      "gate_id": "G-RECORDER-LIFECYCLE",
      "test_notes": "The existing cross-origin-stop smoke test only proves no throw; it does not prove post-stop silence or retention cleanup."
    },
    {
      "id": "junify.lifecycle.replayer-destroy",
      "title": "Replayer destroy releases timers, maps, callbacks, and DOM roots",
      "source": ["observed-code", "explicit-doc"],
      "status": "red-known-risk",
      "recommended_layers": ["integration", "e2e"],
      "entrypoints": ["packages/rrweb/src/replay/index.ts:Replayer.destroy"],
      "observable_effects": ["timer/subscription state", "pending image/stylesheet callbacks", "iframe maps", "removed wrapper", "no callbacks after destroy"],
      "expected_current_behavior": "Repeated create/play/seek/destroy cycles clear all owned asynchronous work and references, not only the visible wrapper.",
      "risk_if_broken": "Rails player navigation accumulates retained recordings, iframes, and callbacks.",
      "gate_id": "G-REPLAYER-LIFECYCLE",
      "test_notes": "The existing destroy test asserts wrapper removal only and does not cover pending resources."
    },
    {
      "id": "junify.package-boundary.player",
      "title": "Scoped Junify player bundles the patched local replayer",
      "source": ["explicit-doc", "external-boundary"],
      "status": "covered",
      "recommended_layers": ["static-analysis", "integration"],
      "entrypoints": ["packages/junify-rrweb-player/package.json", "packages/junify-rrweb-player/vite.config.ts", "packages/rrweb-player/src/main.ts"],
      "observable_effects": ["packed package metadata", "ESM/CJS/UMD exports", "CSS export", "player getReplayer implementation", "observed replay module and CSS paths and digests"],
      "expected_current_behavior": "@junify-app/rrweb-player@2.1.1-junify.0 resolves the patched local Junify replayer and never silently bundles official unpatched @rrweb/replay.",
      "risk_if_broken": "The UI package bypasses the seek fix while appearing to use the Junify player.",
      "gate_id": "G-PKG-PLAYER",
      "test_notes": "Task 3 G-PKG-PLAYER installs exact freshly packed core and player tarballs in an isolated consumer and verifies metadata/files, exact prerelease dependencies, ESM/CJS constructors, real-browser UMD, CSS export/rules, strict declaration resolution, namespace hygiene, and observed local replay module/CSS paths plus SHA-256 digests. A collected mutation proves resolving replay CSS to the official built artifact fails the build. The boundary deliberately builds upstream packages/rrweb-player/src/main.ts because a wrapper breaks Svelte declaration rollup and would be dead code. No artifact was published."
    },
    {
      "id": "junify.package-boundary.rrweb",
      "title": "Only the public Junify rrweb boundary is scoped",
      "source": ["explicit-doc", "external-boundary"],
      "status": "covered",
      "recommended_layers": ["static-analysis", "integration"],
      "entrypoints": ["packages/junify-rrweb/package.json", "packages/junify-rrweb/src/index.ts"],
      "observable_effects": ["packed package metadata", "ESM/CJS/UMD exports", "types", "package tarball contents"],
      "expected_current_behavior": "@junify-app/rrweb@2.1.1-junify.0 exposes record and Replayer while unmodified internals retain official @rrweb/*, rrweb-snapshot, rrdom, and rrweb package identities.",
      "risk_if_broken": "A global namespace rewrite recreates the 315-file divergence and makes future syncs unsafe.",
      "gate_id": "G-PKG-RRWEB",
      "test_notes": "Task 3 G-PKG-RRWEB installs the exact freshly packed tarball in an isolated consumer and verifies metadata/files, ESM/CJS record and Replayer, real-browser UMD global, CSS, strict ESM/CJS declaration resolution, tarball/dependency/DTS namespace hygiene, and observed local source paths plus SHA-256 digests. Exact upstream declaration diagnostic tuples and fresh declaration digests are pinned; collected same-code occurrence and path-drift probes must fail before the skipLibCheck consumer stage. No artifact was published."
    },
    {
      "id": "junify.privacy.persisted-sentinels",
      "title": "Sensitive input values never reach persisted event payloads",
      "source": ["explicit-doc", "observed-code", "external-boundary"],
      "status": "red-known-risk",
      "recommended_layers": ["integration", "e2e"],
      "entrypoints": ["packages/rrweb-snapshot/src/snapshot.ts", "packages/rrweb/src/record/mutation.ts", "../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/index.ts"],
      "observable_effects": ["FullSnapshot JSON", "IncrementalSnapshot JSON", "Chrome storage value", "V1/V2 ingest body"],
      "expected_current_behavior": "Distinct synthetic secrets from password, textarea, placeholder, dynamic type=password, hidden, and sensitive autocomplete cases are absent from initial and mutated persisted payloads.",
      "risk_if_broken": "Credentials or sensitive form values are stored and transmitted despite rendered masking.",
      "gate_id": "G-PRIVACY",
      "test_notes": "Task 2's synthetic emitted-artifact scan confirms password and dynamic-password sentinels are absent, but exposes placeholder, hidden, and sensitive-autocomplete sentinels in every comprehensive fixture; alpha.4 also exposes textarea. Extension storage and V1/V2 request scans plus the Task 6 fix remain release-blocking."
    },
    {
      "id": "junify.rails.legacy-static-player",
      "title": "Legacy static player remains a browser compatibility surface",
      "source": ["observed-code", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["e2e"],
      "entrypoints": ["../junify-rails-rrweb-2.1.1/frontend_rrweb_replayer/index.html", "../junify-rails-rrweb-2.1.1/frontend_rrweb_replayer/replay.js"],
      "observable_effects": ["EVENTS postMessage", "visible replay DOM/Canvas", "META and TIME/SKIP postMessages"],
      "expected_current_behavior": "The alpha.4 static bridge accepts the retained message protocol and replays accepted fixtures with UNSAFE_replayCanvas enabled.",
      "risk_if_broken": "An explicitly retained compatibility surface becomes unusable or diverges silently.",
      "gate_id": "G-RAILS-STATIC",
      "test_notes": "README claims are not coverage; no current browser spec exists and no source reference proves the S3 deployment path is active."
    },
    {
      "id": "junify.rails.monitors-v1-player",
      "title": "Rails Monitors V1 fetch/decode/replay path renders persisted events",
      "source": ["observed-code", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["e2e"],
      "entrypoints": ["../junify-rails-rrweb-2.1.1/frontend_v2/src/api/v2/sessionRecordingAPI.ts:getEvents", "../junify-rails-rrweb-2.1.1/frontend_v2/src/pages/monitors/sessionRecording/useSessionRecordingPlayerEvents.ts", "../junify-rails-rrweb-2.1.1/frontend_v2/src/lib/react-rrweb-player/index.tsx"],
      "observable_effects": ["production-shaped V1 request", "deflate/base64 decode", "visible DOM/Canvas", "seek/play controls"],
      "expected_current_behavior": "The /monitors/session_recordings/:id route fetches V1 pages, decodes every stored event, and renders the expected DOM and Canvas with the candidate player.",
      "risk_if_broken": "Legacy stored sessions fail only in the real Monitors route despite unit tests passing.",
      "gate_id": "G-RAILS-MONITORS-V1",
      "test_notes": "Current parser/loader and player Jest tests mock network or player; no route-level browser gate exists."
    },
    {
      "id": "junify.rails.monitors-v2-player",
      "title": "Rails Monitors V2 replay-plan path renders hot and chunk events",
      "source": ["observed-code", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["e2e", "workflow-contract"],
      "entrypoints": ["../junify-rails-rrweb-2.1.1/frontend_v2/src/api/v3/sessionRecordingAPIV3.ts", "../junify-rails-rrweb-2.1.1/frontend_v2/src/pages/monitors/sessionRecording/playerEventLoader.ts", "../junify-rails-rrweb-2.1.1/frontend_v2/src/pages/monitors/sessionRecording/useSessionRecordingPlayerEvents.ts"],
      "observable_effects": ["replay-plan segment order", "hot-event pagination", "NDJSON chunk decode", "visible DOM/Canvas", "incremental addEvents"],
      "expected_current_behavior": "The Monitors route streams ordered chunk and hot segments, parses raw or compatibility-wrapped rrweb events, and renders the persisted session without gaps or duplicate events.",
      "risk_if_broken": "V2 recordings appear truncated, reordered, stuck, or blank in the production route.",
      "gate_id": "G-RAILS-MONITORS-V2",
      "test_notes": "Current Jest coverage is local/mocked and does not cross the Rails browser route and persisted read boundary."
    },
    {
      "id": "junify.rails.password-rotation-artifact",
      "title": "Password-rotation artifact decompresses and renders in a real player",
      "source": ["existing-test", "observed-code", "external-boundary"],
      "status": "covered",
      "recommended_layers": ["e2e", "workflow-contract"],
      "entrypoints": ["../junify-rails-rrweb-2.1.1/e2e/scenarios/application/member-password-rotation-requests.spec.ts", "../junify-rails-rrweb-2.1.1/frontend_v2/src/components/application/passwordRotation/SessionRecordingPlayerModal.tsx"],
      "observable_effects": ["artifact metadata fetch", "Base64+gzip file fetch", "visible recorded DOM", "interactive player controls"],
      "expected_current_behavior": "A production-shaped capability-run session_recording artifact is fetched, Base64/gzip-decoded, and visibly replayed with usable controls.",
      "risk_if_broken": "Password-rotation evidence cannot be reviewed even though the artifact exists.",
      "gate_id": "G-RAILS-PASSWORD-ARTIFACT",
      "test_notes": "Executable browser coverage exists and the repository records this exact gate; Task 1 did not rerun it. The synthetic fixture has no producer package/version provenance and does not cover the Monitors surfaces."
    },
    {
      "id": "junify.replay.seek-visible-dom",
      "title": "Explicit forward/backward seek uses the correct real DOM",
      "source": ["observed-code", "explicit-doc"],
      "status": "covered",
      "recommended_layers": ["integration", "golden/replay"],
      "entrypoints": ["packages/rrweb/src/replay/index.ts:Replayer.play", "packages/rrweb/src/replay/index.ts:Replayer.pause"],
      "observable_effects": ["post-seek iframe DOM", "current replay time"],
      "expected_current_behavior": "An explicit play at a different forward or backward offset does not use a stale virtual DOM for the first synchronization after the seek.",
      "risk_if_broken": "Reviewers see DOM state from the wrong moment immediately after seeking.",
      "gate_id": "G-SEEK",
      "test_notes": "Task 4 G-SEEK proves an authentic unmodified-2.1.1 RED and candidate GREEN for forward and backward seeks in real Chrome. The focused legacy-sibling fixture observes public current time plus visible iframe DOM; the authenticated alpha.4, Junify alpha.19, Junify alpha.20, and official 2.1.1 artifacts each pass both candidate seek directions. A 13.6 MB FullSnapshot rebuild also preserves CSSOM rule count and visible DOM while recording an official/candidate timing differential."
    },
    {
      "id": "junify.scope.no-compact-serializer",
      "title": "Datadog compact serialization is excluded",
      "source": ["explicit-doc", "external-boundary"],
      "status": "out-of-scope",
      "recommended_layers": [],
      "entrypoints": ["docs/junify/vendor-adoption.md"],
      "observable_effects": ["event wire format", "codec architecture"],
      "expected_current_behavior": "This upgrade does not introduce Datadog compact serialization, semantic streams, or a generalized codec framework.",
      "risk_if_broken": "A recorder upgrade becomes an unplanned system-wide storage and replayer migration.",
      "gate_id": "G-NO-COMPACT-SERIALIZER",
      "test_notes": "Separate architecture story; no implementation in this patch stack."
    },
    {
      "id": "junify.scope.no-cross-origin-expansion",
      "title": "Cross-origin iframe recording remains disabled",
      "source": ["explicit-doc", "observed-code"],
      "status": "out-of-scope",
      "recommended_layers": [],
      "entrypoints": ["../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/index.ts"],
      "observable_effects": ["recordCrossOriginIframes recorder option"],
      "expected_current_behavior": "The extension continues to set recordCrossOriginIframes=false; this upgrade does not widen capture scope.",
      "risk_if_broken": "The upgrade expands privacy/security exposure beyond the approved scope.",
      "gate_id": "G-NO-CROSS-ORIGIN",
      "test_notes": "The focused option test is added with the extension update; feature expansion remains excluded."
    },
    {
      "id": "junify.scope.no-external-release",
      "title": "Inventory and implementation perform no external release action",
      "source": ["explicit-doc"],
      "status": "out-of-scope",
      "recommended_layers": [],
      "entrypoints": ["docs/junify/upstream-sync.md"],
      "observable_effects": ["npm registry", "Git remotes", "deployment environments", "staging"],
      "expected_current_behavior": "No package publish, release, push, pull request, deploy, or staging action occurs in this work.",
      "risk_if_broken": "Unverified package content reaches consumers or production.",
      "gate_id": "G-NO-EXTERNAL-RELEASE",
      "test_notes": "Release operations require separate explicit authorization."
    },
    {
      "id": "junify.service.unused-rrweb-dependency",
      "title": "session_recording_service rrweb@^0.9.14 declaration is not a runtime consumer",
      "source": ["observed-code", "observed-build-artifact"],
      "status": "out-of-scope",
      "recommended_layers": ["static-analysis"],
      "entrypoints": ["/Users/takashihamada/dev/session_recording_service/package.json", "/Users/takashihamada/dev/session_recording_service/.serverless/build/package.json", "/Users/takashihamada/dev/session_recording_service/.serverless/session-recording.zip"],
      "observable_effects": ["source import graph", "Serverless bundle", "packaged service dependencies"],
      "expected_current_behavior": "The stale rrweb@^0.9.14 declaration has no source import/require/dynamic import and is absent from the built package manifest and deployment zip.",
      "risk_if_broken": "An unrecognized runtime consumer is omitted from the compatibility rollout.",
      "gate_id": "G-SRS-UNUSED",
      "test_notes": "All three censuses currently return no runtime occurrence. Removing the declaration is unrelated cleanup and intentionally excluded."
    },
    {
      "id": "junify.transport.v1",
      "title": "V1 event transport preserves per-event deflate/base64 JSON",
      "source": ["observed-code", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["integration"],
      "entrypoints": ["../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/flushSesssionRecordingEventsToServer.ts:transformSessionEventForRequestV1", "../browser-extension-rrweb-2.1.1/src/background/service/junifyAPIClient.ts:postSessionRecordingEventsV1"],
      "observable_effects": ["eventString deflate/base64 round trip", "timestamp/index", "firstEvent", "V1 bulk request"],
      "expected_current_behavior": "Each V1 event is JSON-stringified, fflate-compressed, base64-encoded, and sent with its original timestamp/index and first-event metadata.",
      "risk_if_broken": "Legacy ingest or Rails V1 playback receives corrupt or misindexed events.",
      "gate_id": "G-TRANSPORT-V1",
      "test_notes": "Unit coverage exists, but the required production-shaped persisted round trip remains open."
    },
    {
      "id": "junify.transport.v1-v2-parity",
      "title": "V1 and V2 transports preserve the same logical event stream",
      "source": ["explicit-doc", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["integration", "golden/replay", "workflow-contract"],
      "entrypoints": ["../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/flushSesssionRecordingEventsToServer.ts", "../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/v2SessionRecordingFlusher.ts"],
      "observable_effects": ["decoded event order/count", "FullSnapshot classification", "chunk/fragment reassembly", "gzip digest"],
      "expected_current_behavior": "After decoding/reassembly, V1 and V2 represent the same fixed-scenario rrweb events apart from V2 transport metadata and its synthetic recording_end event.",
      "risk_if_broken": "Feature-flag selection changes what users can replay instead of only how events are transported.",
      "gate_id": "G-TRANSPORT-PARITY",
      "test_notes": "There is no current real artifact parity gate across both pipelines."
    },
    {
      "id": "junify.transport.v2",
      "title": "V2 raw-event transport preserves ordering, classification, and fragments",
      "source": ["observed-code", "external-boundary"],
      "status": "must-cover",
      "recommended_layers": ["integration"],
      "entrypoints": ["../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/v2SessionRecordingFlusher.ts", "../browser-extension-rrweb-2.1.1/src/background/service/junifyAPIClient.ts:postSessionRecordingEventsV2"],
      "observable_effects": ["monotonic assigned indexes", "full_snapshot/incremental classification", "plain eventString", "UTF-8-safe fragment hashes and reassembly", "recording_end"],
      "expected_current_behavior": "V2 sorts source events, assigns monotonic 1-based indexes, stores raw JSON eventString, classifies snapshots, fragments oversized UTF-8 bodies without splitting surrogate pairs, and finalizes with recording_end.",
      "risk_if_broken": "V2 replay plans contain gaps, duplicate indexes, bad hashes, or unreconstructable snapshots.",
      "gate_id": "G-TRANSPORT-V2",
      "test_notes": "Unit tests do not by themselves close the browser-to-persisted-artifact contract."
    }
  ]
}
