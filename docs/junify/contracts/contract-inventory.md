<!-- markdownlint-disable MD013 MD043 -->

# Junify rrweb Compatibility Contract Inventory

## Scope And Evidence Date

This inventory covers the recorder/replayer compatibility boundary for the
clean rebuild from `rrweb@2.1.1^{}` at
`3deb6e7da4528ddb33b5b7ff6a3e805d4ed14930`. Evidence was inspected on
2026-08-18 at rrweb `22f4dea0` (package source `9f575f92`), browser
`d696e841`, and Rails `459b4341` in the three dedicated worktrees. The service
dependency census was read-only.

The machine-readable source is
[`contract-inventory.json`](contract-inventory.json). It contains the same 24
stable IDs, statuses, recommended layers, entrypoints, sinks, behaviors,
risks, and gates summarized here. The exact commands behind every gate are in
[`test-gates-runbook.md`](test-gates-runbook.md).

Status counts after the final Task 11 refresh are:

<!-- prettier-ignore -->
| Status | Count |
| --- | ---: |
| `covered` | 20 |
| `must-cover` | 0 |
| `red-known-risk` | 0 |
| `out-of-scope` | 4 |
| `accepted-current-behavior` | 0 |
| `not-testable-yet` | 0 |

`red-known-risk` identifies a known high-risk gap that is neither accepted nor
closed. `must-cover` identifies feasible missing coverage without asserting a
known current failure. The twenty `covered` contracts have executable gates;
Task 11 connects their package-local, loaded-MV3, transport, Rails route, and
visible-player evidence through the independent cross-repository matrix.

## Contract Matrix

This matrix is generated from `contract-inventory.json`. Do not edit its
rows by hand; update JSON and run
`node docs/junify/contracts/reconcile-contract-inventory.mjs --write`.

<!-- BEGIN GENERATED CONTRACT MATRIX -->
| ID | Title | Source | Status | Recommended layers | Entrypoints | Observable effects | Expected/current behavior | Exact gate | Risk if broken | Test notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `junify.canvas.mv3-processor` | MV3-compatible injectable Canvas bitmap processor | external-boundary; observed-code; explicit-doc | `covered` | integration; e2e | packages/rrweb/src/record/index.ts:record; ../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/imageBitmapProcessor.ts:createCanvasBitmapProcessor | packaged MV3 worker execution; inline fallback output; serialized Canvas mutations | The Junify package accepts an injected ImageBitmap processor, transfers bitmaps through a packaged worker under MV3 strict CSP, and falls back inline without changing the event schema. | `G-CANVAS-PROCESSOR` | The production extension loses Canvas recording, violates MV3 CSP, or emits incompatible events. | Tasks 5, 9, and 11 close the boundary. The loaded MV3 extension uses the packaged worker under strict CSP, proves the natural SecurityError inline fallback, persists/replays exact Canvas2D and compositor-visible WebGL/ImageBitmap pixels, and completes 5/5 focused browser cases with no skips. Task 11 replays every applicable producer through the candidate and Rails sinks. |
| `junify.canvas.non-destructive-capture` | Canvas capture is non-destructive and closes bitmap resources | explicit-doc; external-boundary; observed-code | `covered` | integration; e2e | packages/rrweb/src/record/observers/canvas/canvas-manager.ts; ../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/canvasBitmapWorker.ts | application Canvas pixels; replayed Canvas pixels; ImageBitmap lifetime; worker lifetime | Recording leaves application Canvas 2D/WebGL pixels unchanged, reproduces pixels on replay, closes transferred bitmaps, and stops worker/RAF activity when recording stops. | `G-CANVAS-PIXELS` | Recording mutates customer pages or leaks GPU/bitmap resources during long sessions. | Tasks 5, 7, 9, and 11 prove source pixels are unchanged, persisted and replayed Canvas2D/WebGL pixels are exact, locally owned ImageBitmaps close, pagehide/stop disposes worker and RAF ownership, and corrupt output fails visibly. The loaded-MV3 suite passes 5/5 with no skips and the cross-repository matrix retains the visible sinks. |
| `junify.compatibility.candidate-recorder` | Candidate recorder replays in both candidate Junify surfaces | explicit-doc; external-boundary | `covered` | e2e; golden/replay | packages/junify-compatibility/test/record-fixtures.test.ts; ../junify-rails-rrweb-2.1.1/frontend_v2/src/pages/monitors/sessionRecording/player.tsx; ../junify-rails-rrweb-2.1.1/frontend_rrweb_replayer/replay.js | main-player replayed DOM and Canvas; legacy-static-player replayed DOM and Canvas | Artifacts from @junify-app/rrweb@2.1.1-junify.0 replay in the upgraded Rails main player and the explicitly retained static-player surface. | `G-COMPAT-CANDIDATE` | Deploying the recorder after the replayers creates recordings that Junify cannot inspect. | Task 11 binds candidate-recordings-d696e84-canonical-9f575f92-attempt2 to manifest SHA-256 1ebe9f6399743f91be9878328f7e2304a5be2bf01bd157222aba4680f2ff0357 and follows committed loaded-MV3 raw IDB through V1/V2 transport, Rails loaders/routes, scoped player, direct player, and legacy static visible sinks. V1/V2 have 44/43 events and four FullSnapshots each; the large row has one. Four V1/V2 pagehide/explicit-stop rows freeze IDB and transport sinks after the required pre-shutdown marker. Applicable DOM, Shadow DOM, Canvas2D, WebGL/ImageBitmap, seek, and lifecycle assertions pass with no skips. |
| `junify.compatibility.historical-replay` | Historical alpha.4, alpha.19, and alpha.20 artifacts remain replayable | explicit-doc; external-boundary | `covered` | e2e; golden/replay | packages/junify-compatibility/test/replay-matrix.test.ts; ../junify-rails-rrweb-2.1.1/frontend_v2/src/lib/react-rrweb-player/index.tsx; ../junify-rails-rrweb-2.1.1/frontend_v2/src/lib/react-rrweb-player/PlayerFrame.tsx; ../junify-rails-rrweb-2.1.1/frontend_rrweb_replayer/replay.js | visible replay DOM; Canvas pixels; seek result; player completion | The candidate replayers consume provenance-locked fixtures produced by rrweb@2.0.0-alpha.4, @junify-app/rrweb@2.0.0-alpha.19, and @junify-app/rrweb@2.0.0-alpha.20. | `G-COMPAT-HISTORICAL` | Existing stored customer sessions become unreadable after the player upgrade. | Tasks 2, 4, 10, and 11 authenticate alpha.4, Junify alpha.19, and Junify alpha.20 independently, replay them through the candidate core and every applicable Rails V1/V2/legacy surface, and assert visible DOM, Shadow DOM, Canvas2D, compositor-visible WebGL/ImageBitmap, forward/backward seek, and lifecycle behavior with explicit applicability and no guarded assertions or skips. |
| `junify.compatibility.large-snapshot` | Approximately 13.6 MB CSS FullSnapshot survives transport and replay | explicit-doc; external-boundary | `covered` | integration; golden/replay | packages/junify-compatibility/test/large-snapshot.test.ts; ../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/v2SessionRecordingFlusher.ts | event count; FullSnapshot indexes; fragment reassembly; payload digest; gzip round trip | A deterministic roughly 13.6 MB CSS FullSnapshot preserves decoded event count, indexes, bytes, fragment ordering, and gzip digest. | `G-LARGE-SNAPSHOT` | Large real pages produce truncated, reordered, or unreplayable sessions. | Tasks 2, 9, 10, and 11 prove the exact 13,600,000-byte UTF-8 CSS source, 22 persisted events, FullSnapshot source index 1 and transport index 2, 13,613,688-byte body SHA-256 725fc2763c37c10c9fe3a712f0b170ecc52ee67884eb3458b89285a5b44a6694, 137 contiguous V2 fragments, exact reassembly, gzip round trip, and visible replay. Fragment and payload mutations fail closed. |
| `junify.compatibility.wire-format` | Serialized rrweb event format remains unchanged | explicit-doc; external-boundary | `covered` | integration; golden/replay | packages/rrweb/src/record/index.ts:record; packages/junify-compatibility/test/record-fixtures.test.ts | decoded event sequence; event count; FullSnapshot payload; event JSON schema | For fixed scenarios, decoded events remain wire-compatible; only documented upstream correctness changes may alter payload values. | `G-WIRE-FORMAT` | The service and existing replayers reject or misinterpret newly recorded sessions. | Task 11 combines the independent producer ledger with exact candidate raw/decoded/reassembled event counts, indexes, payload bytes, and SHA-256 values across IDB, V1, V2, Rails parsers/loaders/routes, and visible players. The accepted schema is unchanged; package, census, privacy, fragmentation, and visible-sink rollback probes reach RED and return to GREEN. |
| `junify.extension.persistence` | Extension events reach real Chrome storage before ingest | observed-code; external-boundary | `covered` | integration; e2e; workflow-contract | ../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/index.ts:post; ../browser-extension-rrweb-2.1.1/src/foreground/service/sso/sessionRecorder.ts:SessionRecorder; ../browser-extension-rrweb-2.1.1/src/background/repository/SessionRecordingEventRepository.ts:SessionRecordingEventRepositoryImpl | per-recorder CustomEvent; runtime port payload; Chrome storage keys and values; delete/retry behavior | Monotonic indexed events cross the isolated-world bridge, runtime port, and real Chrome storage without loss, duplication, or reordering before V1/V2 flushing. | `G-EXT-PERSISTENCE` | Unit-green recording silently loses or reorders persisted production events. | Task 9's committed loaded-MV3 suite and Task 11 evidence exercise the real isolated-world bridge, runtime port, IndexedDB repository, retry/delete lifecycle, V1/V2 flushing, and visible downstream players. Candidate raw storage preserves monotonic event identity/order and exact parity; 5/5 loaded-MV3 tests pass with no skips. |
| `junify.lifecycle.recorder-stop` | Recorder stop releases all observers and asynchronous Canvas work | observed-code; explicit-doc | `covered` | integration; e2e | packages/rrweb/src/record/index.ts:record; packages/rrweb/src/record/observer.ts:initObservers; packages/rrweb/src/record/observers/canvas/canvas-manager.ts; packages/rrweb/test/record/lifecycle.test.ts | no events after stop; listener/observer counts; Canvas RAF and worker shutdown; released iframe/stylesheet/shadow-root references | Stop is idempotent, emits nothing afterward, and repeated start/stop churn does not retain observers, mirrors, RAF callbacks, workers, or detached documents. | `G-RECORDER-LIFECYCLE` | Long sessions leak memory/CPU and stopped sessions continue collecting customer activity. | Task 7 plus fix round 1 add real-Chrome source-to-sink gates. Baseline, first, last, and final resource counts are zero across 50 cycles; ten pending-stylesheet cycles retain zero listeners/timers; events remain unchanged after stop and dynamic-password literals remain absent. Focused fixtures prove iframe pagehide/navigation/removal generation disposal, iterative permanent mirror metadata release, shared constructed-stylesheet final-owner release and re-adoption, removed shadow-root silence with replacement liveness, Canvas stop integration, independent cleanup-throw containment, and WebKit untainted-MutationObserver stop/restart. A no-op releaseHost mutation fails the strengthened final-owner gate. |
| `junify.lifecycle.replayer-destroy` | Replayer destroy releases timers, maps, callbacks, and DOM roots | observed-code; explicit-doc | `covered` | integration; e2e | packages/rrweb/src/replay/index.ts:Replayer.destroy; packages/rrweb/src/replay/media/index.ts:MediaManager.destroy; packages/rrweb/test/replay/lifecycle.test.ts; packages/rrweb/test/replayer.test.ts; packages/junify-compatibility/test/replay-matrix.test.ts | Timer action and RAF state; player/speed/media subscription state; stylesheet load and media metadata listeners; image/canvas/document/legacy maps and queues; mirror IDs and removed iframe wrapper; no callback, event, or DOM mutation after destroy | Repeated create/play/seek/destroy cycles clear all owned asynchronous work and references, not only the visible wrapper. | `G-REPLAYER-LIFECYCLE` | Rails player navigation accumulates retained recordings, iframes, and callbacks. | Task 8 adds a real-Chrome 50-cycle create/play/forward-seek/backward-seek/live/destroy gate. Every cycle starts with four nested iframes, an active Timer action/RAF, fifteen emitter handlers, pending stylesheet and media-metadata listeners, and populated image/canvas/document/legacy/media/mirror sinks. Destroy is idempotent and exception-contained; afterward both state machines are stopped, every count and listener balance is zero, the wrapper is detached, late callbacks/events/DOM mutations remain zero, and final tracked listeners/RAF/timeouts/intervals equal the zero baseline. Review correction ccf6c1f3 adds an authentic throwing-cancelAnimationFrame gate: Timer state/actions invalidate before cancellation and an uncancelled stale RAF runs no callback. Exact malformed legacy media and absent style-rules artifacts survive a temporary JSON persistence round trip while following valid media/style events still reach visible sinks. The Task 4 seek and four-producer historical matrix remain green. |
| `junify.package-boundary.player` | Scoped Junify player bundles the patched local replayer | explicit-doc; external-boundary | `covered` | static-analysis; integration | packages/junify-rrweb-player/package.json; packages/junify-rrweb-player/vite.config.ts; packages/rrweb-player/src/main.ts; packages/junify-rrweb/scripts/canonical-packages.mjs | packed package metadata; ESM/CJS/UMD exports; CSS export; player getReplayer implementation; observed replay module and CSS paths and digests; deterministic production archive and unpacked tree identity | @junify-app/rrweb-player@2.1.1-junify.0 resolves the patched local Junify replayer and never silently bundles official unpatched @rrweb/replay. | `G-PKG-PLAYER` | The UI package bypasses the seek fix while appearing to use the Junify player. | Task 3 G-PKG-PLAYER installs exact freshly packed core and player tarballs in an isolated consumer and verifies metadata/files, exact prerelease dependencies, ESM/CJS constructors, real-browser UMD, CSS export/rules, strict declaration resolution, namespace hygiene, and observed local replay module/CSS paths plus SHA-256 digests. A collected mutation proves resolving replay CSS to the official built artifact fails the build. Fix round 3 adds an executable pre/post 18-file content and metadata tree gate around that failing probe, isolates its output from the live player dist, and removes declaration generation from the probe. A Node 20.9 canonical pipeline performs two fresh NODE_ENV=production builds and npm pack --ignore-scripts runs, failing on any archive SHA-256/SHA-512/size or unpacked census/tree drift. Fix round 4 captures a clean HEAD before output, validates the same HEAD plus exact tracked/untracked and generated-residue policy after every clean/build/pack stage and before emission, and uses exactly one npm pack process per run for both absolute package directories with two exact JSON results. Fix round 5 mutation-proves that removing the upstream player scan root misses its two Git-ignored declaration/tsbuild residues; the seven-path sentinel requires exact scanner diagnostics, retain-on-reject behavior, no canonical output, and complete test cleanup. The round 4 artifact bytes remain deterministic but its manifest is superseded for evidence lineage. The prior c4bd708d/6b8aaaf9 player remains quarantined; every consumer must revalidate the newest canonical artifact. The boundary deliberately builds upstream packages/rrweb-player/src/main.ts because a wrapper breaks Svelte declaration rollup and would be dead code. No artifact was published. |
| `junify.package-boundary.rrweb` | Only the public Junify rrweb boundary is scoped | explicit-doc; external-boundary | `covered` | static-analysis; integration | packages/junify-rrweb/package.json; packages/junify-rrweb/src/index.ts; packages/junify-rrweb/scripts/canonical-packages.mjs | packed package metadata; ESM/CJS/UMD exports; types; package tarball contents; deterministic production archive and unpacked tree identity | @junify-app/rrweb@2.1.1-junify.0 exposes record and Replayer while unmodified internals retain official @rrweb/*, rrweb-snapshot, rrdom, and rrweb package identities. | `G-PKG-RRWEB` | A global namespace rewrite recreates the 315-file divergence and makes future syncs unsafe. | Task 3 G-PKG-RRWEB installs the exact freshly packed tarball in an isolated consumer and verifies metadata/files, ESM/CJS record and Replayer, real-browser UMD global, CSS, strict ESM/CJS declaration resolution, tarball/dependency/DTS namespace hygiene, and observed local source paths plus SHA-256 digests. Exact declaration diagnostic tuples and fresh declaration digests are pinned; collected same-code occurrence and path-drift probes must fail before the skipLibCheck consumer stage. Fix round 2 adds the browser consumer's exact literal `{ password: true, textarea: true, hidden: true } satisfies NonNullable<Parameters<typeof record>[0]>['maskInputOptions']` compile contract. The public rrweb type intersects the official `rrweb-snapshot@2.1.1` type with `hidden?: boolean`; the official package identity/dependency remains unchanged and no Junify snapshot package exists. Fix round 3 standardizes both boundaries on the same Node 20.9, NODE_ENV=production, npm pack --ignore-scripts pipeline and requires two exact archive and tree identities. Fix round 4 rejects dirty source, generated boundary type/Svelte/tsbuild residue, and HEAD drift before copying any canonical output. Each production run uses one npm pack process with both exact package paths and results. Fix round 5 adds mutation-backed exact coverage for the two ignored upstream player declaration/tsbuild residue surfaces in addition to the five boundary paths. The prior e075ed25 core archive and round 4 manifest are superseded; consumers must revalidate the newest canonical artifact set. No artifact was published. |
| `junify.privacy.persisted-sentinels` | Sensitive input values never reach persisted event payloads | explicit-doc; observed-code; external-boundary | `covered` | integration; e2e | packages/rrweb-snapshot/src/snapshot.ts; packages/rrweb/src/record/mutation.ts; ../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/index.ts | FullSnapshot JSON; IncrementalSnapshot JSON; Chrome storage value; V1/V2 ingest body | Distinct synthetic secrets from password, textarea, placeholder, dynamic type=password, hidden, and sensitive autocomplete cases are absent from initial and mutated persisted payloads. | `G-PRIVACY` | Credentials or sensitive form values are stored and transmitted despite rendered masking. | Tasks 6, 9, and 11 prove all 16 distinct candidate privacy sentinels absent from committed raw IDB, decoded V1, V2 raw/reassembled payloads, and gzip round trips, with exact node/source/value and missing-event sensitivity probes. Historical leak fixtures remain compatibility-only and are not counted as privacy success. |
| `junify.rails.legacy-static-player` | Legacy static player remains a browser compatibility surface | observed-code; external-boundary | `covered` | e2e | ../junify-rails-rrweb-2.1.1/frontend_rrweb_replayer/index.html; ../junify-rails-rrweb-2.1.1/frontend_rrweb_replayer/replay.js | EVENTS postMessage; visible replay DOM/Canvas; META and TIME/SKIP postMessages | The alpha.4 static bridge accepts the retained message protocol and replays accepted fixtures with UNSAFE_replayCanvas enabled. | `G-RAILS-STATIC` | An explicitly retained compatibility surface becomes unusable or diverges silently. | Tasks 10 and 11 prove the retained static surface with 15/15 focused unit cases and 1/1 fresh Chromium browser case, using the canonical core artifact. Every applicable accepted producer reaches visible DOM/Canvas/seek/lifecycle sinks; the static surface is explicitly N/A for V2 route semantics. |
| `junify.rails.monitors-v1-player` | Rails Monitors V1 fetch/decode/replay path renders persisted events | observed-code; external-boundary | `covered` | e2e | ../junify-rails-rrweb-2.1.1/frontend_v2/src/api/v2/sessionRecordingAPI.ts:getEvents; ../junify-rails-rrweb-2.1.1/frontend_v2/src/pages/monitors/sessionRecording/useSessionRecordingPlayerEvents.ts; ../junify-rails-rrweb-2.1.1/frontend_v2/src/lib/react-rrweb-player/index.tsx | production-shaped V1 request; deflate/base64 decode; visible DOM/Canvas; seek/play controls | The /monitors/session_recordings/:id route fetches V1 pages, decodes every stored event, and renders the expected DOM and Canvas with the candidate player. | `G-RAILS-MONITORS-V1` | Legacy stored sessions fail only in the real Monitors route despite unit tests passing. | Tasks 10 and 11 prove the production-shaped V1 parser/loader/route and direct/scoped visible player with a fresh dedicated local-only VM. The V1/V2 matrix passes 2/2 with zero skipped, unexpected, flaky, or retried tests, and all four accepted producers have an explicit V1 row. |
| `junify.rails.monitors-v2-player` | Rails Monitors V2 replay-plan path renders hot and chunk events | observed-code; external-boundary | `covered` | e2e; workflow-contract | ../junify-rails-rrweb-2.1.1/frontend_v2/src/api/v3/sessionRecordingAPIV3.ts; ../junify-rails-rrweb-2.1.1/frontend_v2/src/pages/monitors/sessionRecording/playerEventLoader.ts; ../junify-rails-rrweb-2.1.1/frontend_v2/src/pages/monitors/sessionRecording/useSessionRecordingPlayerEvents.ts | replay-plan segment order; hot-event pagination; NDJSON chunk decode; visible DOM/Canvas; incremental addEvents | The Monitors route streams ordered chunk and hot segments, parses raw or compatibility-wrapped rrweb events, and renders the persisted session without gaps or duplicate events. | `G-RAILS-MONITORS-V2` | V2 recordings appear truncated, reordered, stuck, or blank in the production route. | Tasks 10 and 11 prove V2 fragment parsing, exact reassembly, route loading, and visible scoped/direct player behavior in a fresh dedicated local-only VM. The V1/V2 matrix passes 2/2 with zero skipped, unexpected, flaky, or retried tests; candidate and 13.6 MB artifacts retain exact count/index/payload parity. |
| `junify.rails.password-rotation-artifact` | Password-rotation artifact decompresses and renders in a real player | existing-test; observed-code; external-boundary | `covered` | e2e; workflow-contract | ../junify-rails-rrweb-2.1.1/e2e/scenarios/application/member-password-rotation-requests.spec.ts; ../junify-rails-rrweb-2.1.1/frontend_v2/src/components/application/passwordRotation/SessionRecordingPlayerModal.tsx | artifact metadata fetch; Base64+gzip file fetch; visible recorded DOM; interactive player controls | A production-shaped capability-run session_recording artifact is fetched, Base64/gzip-decoded, and visibly replayed with usable controls. | `G-RAILS-PASSWORD-ARTIFACT` | Password-rotation evidence cannot be reviewed even though the artifact exists. | Executable browser coverage exists and the repository records this exact gate; Task 1 did not rerun it. The synthetic fixture has no producer package/version provenance and does not cover the Monitors surfaces. |
| `junify.replay.seek-visible-dom` | Explicit forward/backward seek uses the correct real DOM | observed-code; explicit-doc | `covered` | integration; golden/replay | packages/rrweb/src/replay/index.ts:Replayer.play; packages/rrweb/src/replay/index.ts:Replayer.pause | post-seek iframe DOM; current replay time | An explicit play at a different forward or backward offset does not use a stale virtual DOM for the first synchronization after the seek. | `G-SEEK` | Reviewers see DOM state from the wrong moment immediately after seeking. | Task 4 G-SEEK proves an authentic unmodified-2.1.1 RED and candidate GREEN for forward and backward seeks in real Chrome. The focused legacy-sibling fixture observes public current time plus visible iframe DOM; the authenticated alpha.4, Junify alpha.19, Junify alpha.20, and official 2.1.1 artifacts each pass both candidate seek directions. A 13.6 MB FullSnapshot rebuild also preserves CSSOM rule count and visible DOM while recording an official/candidate timing differential. |
| `junify.scope.no-compact-serializer` | Datadog compact serialization is excluded | explicit-doc; external-boundary | `out-of-scope` | none | docs/junify/vendor-adoption.md | event wire format; codec architecture | This upgrade does not introduce Datadog compact serialization, semantic streams, or a generalized codec framework. | `G-NO-COMPACT-SERIALIZER` | A recorder upgrade becomes an unplanned system-wide storage and replayer migration. | Separate architecture story; no implementation in this patch stack. |
| `junify.scope.no-cross-origin-expansion` | Cross-origin iframe recording remains disabled | explicit-doc; observed-code | `out-of-scope` | none | ../browser-extension-rrweb-2.1.1/src/injected/sessionRecordingClient/index.ts | recordCrossOriginIframes recorder option | The extension continues to set recordCrossOriginIframes=false; this upgrade does not widen capture scope. | `G-NO-CROSS-ORIGIN` | The upgrade expands privacy/security exposure beyond the approved scope. | The focused option test is added with the extension update; feature expansion remains excluded. |
| `junify.scope.no-external-release` | Inventory and implementation perform no external release action | explicit-doc | `out-of-scope` | none | docs/junify/upstream-sync.md | npm registry; Git remotes; deployment environments; staging | No package publish, release, push, pull request, deploy, or staging action occurs in this work. | `G-NO-EXTERNAL-RELEASE` | Unverified package content reaches consumers or production. | Release operations require separate explicit authorization. |
| `junify.service.unused-rrweb-dependency` | session_recording_service rrweb@^0.9.14 declaration is not a runtime consumer | observed-code; observed-build-artifact | `out-of-scope` | static-analysis | /Users/takashihamada/dev/session_recording_service/package.json; /Users/takashihamada/dev/session_recording_service/.serverless/build/package.json; /Users/takashihamada/dev/session_recording_service/.serverless/session-recording.zip | source import graph; Serverless bundle; packaged service dependencies | The stale rrweb@^0.9.14 declaration has no source import/require/dynamic import and is absent from the built package manifest and deployment zip. | `G-SRS-UNUSED` | An unrecognized runtime consumer is omitted from the compatibility rollout. | All three censuses currently return no runtime occurrence. Removing the declaration is unrelated cleanup and intentionally excluded. |
| `junify.transport.v1` | V1 event transport preserves per-event deflate/base64 JSON | observed-code; external-boundary | `covered` | integration | ../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/flushSesssionRecordingEventsToServer.ts:transformSessionEventForRequestV1; ../browser-extension-rrweb-2.1.1/src/background/service/junifyAPIClient.ts:postSessionRecordingEventsV1 | eventString deflate/base64 round trip; timestamp/index; firstEvent; V1 bulk request | Each V1 event is JSON-stringified, fflate-compressed, base64-encoded, and sent with its original timestamp/index and first-event metadata. | `G-TRANSPORT-V1` | Legacy ingest or Rails V1 playback receives corrupt or misindexed events. | Tasks 9-11 exercise real committed MV3 IDB to decoded V1 request bodies and the Rails V1 parser/loader/route. Candidate V1 retains 44 events, FullSnapshot indexes 1/16/18/21, and exact 50,016-byte SHA-256 96f50179895713b99066b9cba64bb5d5f7c704e0a15d2655eca6c1cf1217e718; privacy and payload mutations fail. |
| `junify.transport.v1-v2-parity` | V1 and V2 transports preserve the same logical event stream | explicit-doc; external-boundary | `covered` | integration; golden/replay; workflow-contract | ../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/flushSesssionRecordingEventsToServer.ts; ../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/v2SessionRecordingFlusher.ts | decoded event order/count; FullSnapshot classification; chunk/fragment reassembly; gzip digest | After decoding/reassembly, V1 and V2 represent the same fixed-scenario rrweb events apart from V2 transport metadata and its synthetic recording_end event. | `G-TRANSPORT-PARITY` | Feature-flag selection changes what users can replay instead of only how events are transported. | Tasks 9-11 compare canonical event identity/order and exact payload/index semantics across real IDB, V1, V2 raw fragments, reassembly, gzip, Rails loaders, and visible sinks. Intended transport-envelope differences have explicit applicability; payload drift, producer omission, and sink substitution fail the sensitivity gates. |
| `junify.transport.v2` | V2 raw-event transport preserves ordering, classification, and fragments | observed-code; external-boundary | `covered` | integration | ../browser-extension-rrweb-2.1.1/src/background/command/sessionRecording/v2SessionRecordingFlusher.ts; ../browser-extension-rrweb-2.1.1/src/background/service/junifyAPIClient.ts:postSessionRecordingEventsV2 | monotonic assigned indexes; full_snapshot/incremental classification; plain eventString; UTF-8-safe fragment hashes and reassembly; recording_end | V2 sorts source events, assigns monotonic 1-based indexes, stores raw JSON eventString, classifies snapshots, fragments oversized UTF-8 bodies without splitting surrogate pairs, and finalizes with recording_end. | `G-TRANSPORT-V2` | V2 replay plans contain gaps, duplicate indexes, bad hashes, or unreconstructable snapshots. | Tasks 9-11 exercise real committed MV3 IDB to V2 fragments and Rails V2 reassembly/loading. Candidate V2 retains 43 reassembled events, FullSnapshot indexes 1/16/19/21, and exact 49,644-byte SHA-256 23880484caf23dbbf13debdb75ce643561964f3b4b22709c90745f1a2c778d50; the large artifact is exactly 137 contiguous fragments and reassembles byte-for-byte. |
<!-- END GENERATED CONTRACT MATRIX -->

## Current Consumer Matrix

“Locked” means the exact committed lock integrity and installed package tree,
not merely a manifest range. The unpublished scoped versions were installed
from the canonical local archives for verification; this is not registry proof.

| Consumer at final head                                              | Exact package/content policy                                                                 | Active path                                                             | Task 11 conclusion                                                                                                          |
| ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| browser extension `d696e841088ceeac96511ace124d1079c0f674ba`        | core archive/tree `3ec683a2...`/`f83acf63...`; candidate manifest `1ebe9f63...`              | injected recorder → bridge/runtime port → raw IDB → V1/V2 flushers      | loaded MV3, worker/fallback, persistence, FullSnapshots 4/4/1, four shutdown rows, privacy, and fragmentation are `covered` |
| Rails SPA `459b43416ab80105995656c972514bc93b2ff7d2`                | same core plus player archive/tree `b317a39f...`/`c9929b76...`                               | V1 parser, V2 replay-plan loader, Monitors route, scoped/direct players | dedicated local-only VM passes V1/V2 2/2 actual-browser matrix; final safe gates pass 34/34, 8/8, and 7/7; `covered`        |
| Rails password-rotation player at the same head                     | same canonical package trees                                                                 | artifact endpoint → Base64/gzip decode → visible modal                  | focused unit and actual-browser 2/2; `covered`                                                                              |
| Rails legacy static player at the same head                         | canonical core archive replaces the historical alpha.4 CDN dependency for the verified build | `EVENTS` input → META/TIME/SKIP plus visible replay                     | unit 15/15, production build, actual Chromium 1/1; `covered`; deployment reachability remains undocumented                  |
| candidate package source `9f575f92060049ce9a553427129af30197d787f5` | combined manifest `7efe3c54...`; two byte/tree-identical production runs                     | narrow public core/player packages over official internal identities    | both packed boundaries and consumer-installed tree policies are `covered`; registry/CDN install remains a release blocker   |
| `session_recording_service` read-only shared checkout               | manifest declares `rrweb@^0.9.14`; no runtime occurrence                                     | service persists/transforms raw rrweb JSON independently of npm rrweb   | source/build/zip censuses strongly disprove runtime participation; dependency cleanup remains `out-of-scope`                |

### Recorder Defaults That Must Stay Explicit

The final extension enables Canvas by default at 5 fps using WebP quality 0.6,
records after `load`, takes a full snapshot every five minutes, disables cross-
origin iframe recording, and explicitly enables the approved password,
textarea, hidden, placeholder, and sensitive-autocomplete protections. SPA
`pushState` and `popstate` force full snapshots. Task 11 proves the 16 candidate
sentinels absent at every persisted and transport sink; historical leaks remain
compatibility-only characterization.

## Existing Fixture Matrix

| Fixture/source                                          | What is known                                                                                                                                                             | Provenance gap                                                                                             | Contract use                                                                             |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| service `real-cloudflare-recording-1209-events.json.gz` | 1,209 events, one FullSnapshot at transport index 2, largest `eventString` 535,950 bytes; gzip SHA-256 `1c428ef54bdf45b5b218f1450bbbfe41fe1eac68841df4b7dfb2ace1368690cb` | no producer package/version, browser/version, creation command, uncompressed digest, or privacy assertions | useful service pipeline input only; not an alpha fixture                                 |
| service `real-cloudflare-recording-1227-events.json.gz` | 1,227 events, FullSnapshots at indexes 2 and 1225, largest `eventString` 1,432,290 bytes; gzip SHA-256 `5e317c740381bfb5a8e5a704bf11c313251851653cdfbeb9a8cd615279ed1a6f` | same missing provenance                                                                                    | largest current artifact, still far below roughly 13.6 MB CSS requirement                |
| service `real-cloudflare-recording-1907-events.json.gz` | 1,907 events, FullSnapshots at indexes 2 and 1905, largest `eventString` 1,385,765 bytes; gzip SHA-256 `8bb33b9b0d4b688f91a861766d9f07a11e486371b23594fbccb8ec2e0dd17b24` | same missing provenance                                                                                    | contains DOM/CSS/Canvas/Shadow markers, but is not a privacy or producer-version fixture |
| Rails password-rotation generated artifact              | deterministic two-event Meta + FullSnapshot payload; Base64 wrapping gzip; visible sentinel text                                                                          | hand-built event JSON has no producer package/browser provenance                                           | proves one persisted artifact browser flow only                                          |
| upstream rrweb event/test fixtures                      | broad synthetic replay, input masking, Canvas/WebGL, iframe, Shadow DOM, stylesheet, and seek coverage                                                                    | not Junify consumer configuration, not persisted through extension/service/Rails                           | supports focused local regression tests but cannot close cross-repository contracts      |

New compatibility fixtures must record producer package and exact version,
scenario, browser/version, creation command, uncompressed and gzip SHA-256,
event count, FullSnapshot indexes, and privacy assertions. They must use only
synthetic data and must cover alpha.4, alpha.19, alpha.20, official 2.1.1 as
the unmodified baseline, and candidate `2.1.1-junify.0`.

## Repository/Fork Evidence

- The peeled stable tag is exactly
  `3deb6e7da4528ddb33b5b7ff6a3e805d4ed14930`.
- The merge base with the cached Junify fork is
  `76df9799ecc14930fa914e5623a73ea7726e3747`.
- Local no-fetch refs have `master` and `origin/master` at
  `8824d8f34e7b93597b5c90d9075d183aea111794`, 7 commits ahead and 62 behind
  stable. The coordinator’s independent remote census reports the later remote
  tip `bad4613d6ed...` and 11/62. That object is not present in this no-fetch
  worktree, so the remote claim remains attributed rather than locally
  reverified.
- `af62ec15` changes 315 files for the global namespace rewrite. It is reference
  material, not a replay unit.
- Behavioral reference commits are seek `8d0afa80`, Canvas `45ea914e`, and
  alpha.20 stabilization `8824d8f3`. Raw commits are not cherry-picked.

## Explicit Boundaries

This inventory excludes wire-format changes, Datadog compact serialization,
cross-origin iframe expansion, publishing/releasing/pushing/PRs/deployment/
staging, and cleanup of the service’s unused npm declaration. The legacy static
player remains in compatibility scope until ownership explicitly removes it;
absence of an active-deployment source reference is not evidence that it is
dead.
